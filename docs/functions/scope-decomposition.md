# Область видимости и декомпозиция
<p class="reading-time">Чтение: 7 минут · Практика: 18 минут</p>
<div class="tags"><span class="tag">10–11 класс</span><span class="tag tag--ege">ЕГЭ</span><span class="tag tag--plus">Python+</span></div>
Локальная переменная существует внутри вызова функции. Разбиение задачи на функции уменьшает объём, который надо держать в голове.
!!! abstract "Фокус"
    - **Нужно уметь:** передавать данные параметрами и возвращать результат.
```python
def calculate_result(numbers):
    return sum(numbers)

def main():
    numbers = [3, 5, 7]
    result = calculate_result(numbers)
    print(result)

main()
```
Одинаковые имена внутри и снаружи могут обозначать разные переменные. Глобальное изменяемое состояние затрудняет проверку: предпочитайте параметры и `return`.
## Мини-практика
1. Разделите ввод, вычисление и вывод. 2. Предскажите локальные значения двух вызовов. 3. Уберите глобальный счётчик.
## Проверьте себя
Где живёт локальная переменная? Чем помогает декомпозиция? Почему глобальные значения опасны?
## Где пригодится
- **ЕГЭ:** читаемый код сложных алгоритмов. **Далее:** [рекурсия](../recursion/basics.md).
<div class="topic-nav"><a href="../basics/">← Функции</a><a href="../../recursion/basics/">Рекурсия →</a></div>
