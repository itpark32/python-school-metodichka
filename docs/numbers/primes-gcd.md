# Простые числа, НОД и разложение

<p class="reading-time">Чтение: 9 минут · Практика: 22 минуты</p>
<div class="tags"><span class="tag">9–11 класс</span><span class="tag tag--ege">ЕГЭ</span><span class="tag tag--plus">Углублённый уровень</span></div>

Простое число больше 1 и имеет ровно два делителя. НОД удобно искать алгоритмом Евклида.

!!! abstract "Фокус"
    - **Нужно уметь:** проверить простоту и объяснить Евклида.

```python
def is_prime(number):
    if number < 2:
        return False
    d = 2
    while d * d <= number:
        if number % d == 0:
            return False
        d += 1
    return True
```

Разложение: пока число делится на текущий `d`, сохраняйте `d` и делите число; затем переходите к следующему делителю.

```python
while b != 0:
    a, b = b, a % b
print(a)
```

НОД не меняется, если заменить пару `(a,b)` на `(b,a%b)`, а остатки уменьшаются до нуля.

## Мини-практика

1. Проверьте числа 1, 2, 49. 2. Разложите 84. 3. Найдите НОД 48 и 18.

## Проверьте себя

Почему 1 не простое? Почему достаточно идти до корня? Что означает последний ненулевой остаток?

## Где пригодится

- **ЕГЭ:** целочисленные алгоритмы №25. **Далее:** [системы счисления](bases.md).

<div class="topic-nav"><a href="../divisibility-divisors/">← Делители</a><a href="../bases/">Системы счисления →</a></div>
