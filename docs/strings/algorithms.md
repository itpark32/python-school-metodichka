# Алгоритмы обработки строк
<p class="reading-time">Чтение: 8 минут · Практика: 20 минут</p>
<div class="tags"><span class="tag">9–11 класс</span><span class="tag tag--ege">ЕГЭ</span></div>
Проход по строке хранит состояние текущего фрагмента: счётчик, построенный результат или длину серии.
!!! abstract "Фокус"
    - **Нужно знать:** текущая и максимальная серии.
    - **Нужно уметь:** сбрасывать состояние при нарушении условия.
```python
text = "AAABBCCCCDAA"
current = maximum = 1
for i in range(1, len(text)):
    if text[i] == text[i - 1]:
        current += 1
    else:
        current = 1
    if current > maximum:
        maximum = current
print(maximum)
```
`current` описывает серию, заканчивающуюся в текущей позиции; `maximum` — лучшую из уже увиденных. Для пустой строки нужен отдельный случай. Похожим проходом считают пары и строят новую строку через накопление.
## Мини-практика
1. Посчитайте цифры. 2. Найдите число пар `AB`. 3. Найдите максимальную серию цифр.
## Проверьте себя
Почему цикл начинается с 1? Когда сбрасывается `current`? Что делать с пустой строкой?
## Где пригодится
- **ЕГЭ:** база №24 и [текстовых файлов](../files/text.md). **Далее:** [списки](../lists/basics.md).
<div class="topic-nav"><a href="../methods-slices/">← Методы</a><a href="../../lists/basics/">Списки →</a></div>
