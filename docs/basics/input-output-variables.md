# Ввод, вывод и переменные

<p class="reading-time">Чтение: 6 минут · Практика: 12 минут</p>
<div class="tags"><span class="tag">8 класс</span><span class="tag tag--oge">ОГЭ</span><span class="tag tag--ege">ЕГЭ</span></div>

Программа выполняет команды сверху вниз. Переменная — подписанная ячейка, в которой программа хранит значение.

!!! abstract "Фокус"
    - **Нужно знать:** `print`, `input`, присваивание `=`.
    - **Нужно уметь:** прочитать данные, изменить значение, вывести результат.
    - **Полезно знать:** f-строки делают вывод понятнее.

## 1. Первая программа

```python
name = input("Имя: ")
age = int(input("Возраст: "))
print(f"{name}, через год тебе будет {age + 1}")
```

`input()` всегда возвращает строку. Поэтому возраст превращаем в целое число функцией `int`. В фигурных скобках f-строки можно писать переменные и выражения.

## 2. Присваивание — не равенство

```python
x = 5
x = x + 2
x = x * 3
print(x)
```

После каждой строки старое значение `x` заменяется новым: `5 → 7 → 21`. Запись `x = x + 2` — команда «взять старое `x`, прибавить 2 и сохранить обратно».

## 3. Типовые задачи

```python
price = int(input())
count = int(input())
print(price * count)
```

Для минут: `hours = minutes // 60`, `rest = minutes % 60`. Для двух чисел сохраните каждое отдельно, затем вычислите сумму и произведение.

## 4. Частые ошибки

- `"2" + "3"` даёт строку `"23"`, а не число `5`;
- `=` присваивает, а `==` сравнивает;
- переменную нельзя читать до первого присваивания;
- имена `a`, `b` допустимы в короткой задаче, но `price` понятнее в большой.

## 5. Мини-практика

1. 🟢 Спросите имя и город, выведите одну фразу.
2. 🔵 По цене и количеству найдите стоимость покупки.
3. 🟠 Переведите заданное число минут в часы и минуты.

## 6. Проверьте себя

1. Какой тип возвращает `input()`?
2. Чему равен `x` после `x = 4; x = x + 3`?
3. Чем `=` отличается от `==`?

## Где пригодится

- **В школе:** почти любая программа начинается со ввода и переменных.
- **ОГЭ:** чтение кода и собственная программа №16.
- **ЕГЭ:** основа всех программных решений.
- **Следующая тема:** [типы данных и арифметика](types-arithmetic.md).

<div class="topic-nav"><a href="../index/">← Маршрут</a><a href="../types-arithmetic/">Типы и арифметика →</a></div>
