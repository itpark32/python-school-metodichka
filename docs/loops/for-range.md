# `for` и `range`

<p class="reading-time">Чтение: 6 минут · Практика: 15 минут</p>
<div class="tags"><span class="tag">8 класс</span><span class="tag tag--oge">ОГЭ</span><span class="tag tag--ege">ЕГЭ</span></div>

`for` удобен, когда известны элементы или количество повторов.

!!! abstract "Фокус"
    - **Нужно знать:** `range(stop)`, `range(start, stop, step)`.
    - **Нужно уметь:** выбрать правильные границы.
    - **Полезно знать:** правая граница не включается.

## 1. Диапазоны

```python
for i in range(5):       # 0, 1, 2, 3, 4
    print(i)

for i in range(1, 6):    # 1, 2, 3, 4, 5
    print(i)

for i in range(10, 0, -2):
    print(i)             # 10, 8, 6, 4, 2
```

`range(n)` даёт ровно `n` значений от 0. Чтобы перебрать от 1 до `n` включительно, нужна граница `n + 1`.

```python
n = int(input())
total = 0

for _ in range(n):
    total += int(input())

print(total)
```

Имя `_` показывает: номер повтора нам не нужен.

## 2. Частые ошибки

- ожидать включения правой границы;
- забывать отрицательный шаг при движении назад;
- путать `range(n)` и `range(1, n + 1)`;
- менять счётчик цикла вручную без необходимости.

## 3. Мини-практика

1. 🟢 Выведите числа от 1 до N.
2. 🔵 Найдите сумму от 1 до N.
3. 🟠 Среди N введённых чисел посчитайте кратные 3.

## 4. Проверьте себя

1. Какие значения даёт `range(2, 8, 2)`?
2. Почему используется `n + 1`?
3. Когда лучше `while`, а не `for`?

## Где пригодится

- **В школе:** циклы с известным числом повторов.
- **ОГЭ:** основной цикл собственной программы №16.
- **ЕГЭ:** перебор файлов, строк и диапазонов.
- **Следующая тема:** [вложенные циклы](nested-loops.md).

<div class="topic-nav"><a href="../while/">← while</a><a href="../nested-loops/">Вложенные циклы →</a></div>
