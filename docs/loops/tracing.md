# Трассировка программ

<p class="reading-time">Чтение: 7 минут · Практика: 15 минут</p>
<div class="tags"><span class="tag">8–11 класс</span><span class="tag tag--oge">ОГЭ</span><span class="tag tag--ege">ЕГЭ</span></div>

Трассировка — ручное пошаговое выполнение программы с записью значений переменных.

!!! abstract "Фокус"
    - **Нужно знать:** значения записывают после выполнения строки.
    - **Нужно уметь:** построить таблицу шагов цикла.
    - **Полезно знать:** трассировка полезнее попытки «угадать» ответ.

## 1. Цикл `for`

```python
x = 3
count = 0

for i in range(4):
    x += i
    if x % 2 == 0:
        count += 1
```

| шаг | i | x после `x += i` | `x` чётное | count |
|---:|---:|---:|---|---:|
| 1 | 0 | 3 | нет | 0 |
| 2 | 1 | 4 | да | 1 |
| 3 | 2 | 6 | да | 2 |
| 4 | 3 | 9 | нет | 2 |

## 2. Цикл `while`

Сначала выпишите стартовые значения, затем для каждой итерации: результат проверки, изменения в теле и значение перед следующим условием. Отдельно отметьте шаг, на котором условие стало ложным.

## 3. Частые ошибки

- пропускать значение 0 в `range(4)`;
- проверять условие до изменения `x`, хотя в коде оно стоит после;
- держать несколько шагов в голове вместо таблицы;
- записывать только изменившуюся переменную и терять состояние.

## 4. Мини-практика

1. 🟢 Протрассируйте `x = 1; for i in range(3): x *= 2`.
2. 🟠 Постройте таблицу для цикла до нуля.
3. 🔴 Измените в примере `range(4)` на `range(1, 5)` и сравните ответ.

## 5. Проверьте себя

1. Когда записывать новое значение переменной?
2. Какие значения принимает `i`?
3. Как таблица помогает найти логическую ошибку?

## Где пригодится

- **В школе:** понимание готовых алгоритмов.
- **ОГЭ:** №6.
- **ЕГЭ:** анализ хода алгоритма; в модели 2026 — №23, в проекте 2027 — №13.
- **Следующая тема:** [строки](../strings/basics.md).

<div class="topic-nav"><a href="../sequences/">← Последовательности</a><a href="../../strings/basics/">Строки →</a></div>
